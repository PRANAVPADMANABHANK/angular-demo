import { GreenbackgroundDirective } from './greenbackground.directive';

describe('GreenbackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenbackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
